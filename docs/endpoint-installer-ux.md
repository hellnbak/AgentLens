# Endpoint installer UX

Installers support:

- `--dry-run`
- `--no-start`
- `--immutable`
- environment overrides for upstream endpoint, API key, and resource attributes
- explicit verification commands after install

Run preflight first:

```bash
sudo scripts/preflight-endpoint-install.sh
```
